# 今日項目
### HIOKI LR8400
-  [x] ch選擇
-  [x] 量測取值
-  [x] 斷線重新連線
-  [x] 取消清除cache
-  [x] 區間設定
### ITECH
-  [x] 電池mode
-  [x] CC CV 盡量全用CC做
-  [x] CC CV CP 模式指令
-  [x] 根據測試解果顯示 還是需要協助將其他數值輸入完畢 因此要自動輸入其餘數值
-  [x] 模式開發進度
	-  [x] CC
	-  [x] CV
	-  [x] CP/CW
	-  [x] 保護點設定
		  -  [x] 已經測定 行為模式會立即斷電 多加寫復歸Func
	-  [x] 復歸func
-  [x] 找時間給Rex哥先看一遍
-  [x] 排程設定
-  [x] 主頁面設計
-  [x] Device Data Interface
-  [x] 預接設備(防打火實作)
-  [x] 單工步實作
-  [x] 單工步加入保護點設定
-  [x] 保護點讀取 從API設定
-  [x] 單工步與預接防呆
	-  [x] 預接
		- 設置電壓、電流、功率大於保護點OVP OCP OPP
		- 設置電壓電流小於保護點UCP UVP
	-  [x] 單工步
		- 設置電壓、電流、功率大於保護點OVP OCP OPP
		- 設置電壓電流小於保護點UCP UVP
-  [x] 單公布執行設定並開始後點回去要可以看說正在執行的條件是什麼
-  [x] websocket斷線要解開設備控制 讓設備可以手動控制
-  [x] 工步排程設計
	- 要可以存為JSON參考已有格式
	- 防呆 如：一開始就有END
	- 頁面仿造
	- DeviceCard綠色時按下進入設定
-  [x] 解除Alarm要順便停下排程
-  [x] 不能只有綠色才能看 藍色也要可以點進去看
	-  [x] 改成點下就可以進去排程頁
-  [x] 點兩下進去讀取素梅派狀態與IP
-  [x] current_step_config沒用
- 額外 預接電池功能是否需要重新開發
``` ts
	const submitSetValues = async () => {
	  if (!props.isOnline) return;
	
	  const validationError = validateProtectionRules();
	  if (validationError) {
	    errorMessage.value = validationError;
	    return;
	  }
	
	  isLoading.value = true;
	  errorMessage.value = '';
	
	  try {
	    const protectionPayload: ProtectionReq = {
	      ovp: Number(protectionsForm.value.ovp),
	      uvp: Number(protectionsForm.value.uvp),
	      ocp: Number(protectionsForm.value.ocp),
	      ucp: Number(protectionsForm.value.ucp),
	      opp: Number(protectionsForm.value.opp),
	      delay: Number(protectionsForm.value.delay)
	    };
	
	    await deviceStore.updateProtection(props.deviceId, protectionPayload);
	    // 等等看要不要改
	    await deviceApi.setValues(getBaseUrl(), {
	      voltage: Number(preConnectForm.value.voltage),
	      current: Number(preConnectForm.value.current),
	      power: Number(preConnectForm.value.power)
	    });
	
	    modalStage.value = 'CONFIRM_ON';
	  } catch (err: unknown) {
	    errorMessage.value = err instanceof Error ? err.message : '設定失敗，請檢查網路連線或參數';
	  } finally {
	    isLoading.value = false;
	  }
	};
```
- SCPI 發出聲音代表命令下錯
- 排查一下 直到命令都不會有聲音 跟Error
-  [x] 預接功能改寫成 Barcode表對應
	-  [x] 保留原本介面 變成工程用 ctrl shift E 輸入密碼 1225
	-  [x] 保護點 都0 關閉保護
	-  [x] 輸入 GU2825^^^^^^^ 電壓 電流 自動算出功率 
		- GU2825^^^^^^^ 是為了可以對上 GU2825 跟 str long
	-  [x] 存Pina 一樣永久化 
	-  [x] 做匯入匯出 避免換電腦
	-  [x] 後面點下後是跑出輸入框 先輸入Barcode
	-  [x] 再跑出電壓電流功率數據
-  [x] Step用什麼參數EC EV多少去跑寫清楚一點
-  [x] RUN期間排程頁 All Disable 只剩停止排程 且可以看資料
-  [x] 顏色狀態對齊 區分一下in排成跟out排程 用Step ; Finish 多寫一個狀態![[Pasted image 20260824152852.png]]
-  [x] 測試HIOKI API 並串接到網頁上 讓網頁可以設定通道量測
-  [x] 多加測試看看有沒有Bug
	-  [x] HIOKI type對準
-  [x] HIOKI 加上精準度設計
-  [x] 串接ITECH與HIOKI讓他可以用數值去判斷
	-  [x] 預計作法 給ITECH的API 有哪些通道 要用多少判斷值判斷
-  [x] LOG功能開發
	-  [x] 先可以記錄
	-  [x] 將間隔與存放區間放入伺服器
	-  [x] 可以看到有什麼資料夾
	-  [x] 可以看到有什麼檔案 可以預覽
	-  [x] 可以拉檔
	-  [x] 執行續時間timp統一
-  [ ] 開始時要跳出視窗輸入Barcode或工程版面
	- 先做工程版面：要讓使用者填入要上傳的Json位置、 Log name、Logpath
	- 做完後要想辦法讓他可以自動下載到指定位置
	- 要存在pinia 如果突然斷線回來後看到做完了要自己拉檔案
	- 要拉到指定logpath位置不能只是
-  [ ] 防呆邏輯
	-  [x] 保護點比截止還低或高
	-  [x] 切換工步的時候 這個工步有修改但未存要提示
	-  [x] 離開同上原理 離開前有修改要問是否要先上傳排程
-  [ ] 要讓HIOKI記住是誰開啟的
	- 以後多通道可能是很多不同使用者使用 為了減少開關的問題 要讓API存取在HIOKI的伺服器內 讓他可以確認這次關閉會不會影響到其他使用者 簡單來說說 第一個開啟API才是開 最後一個關閉API才是關
-  [ ] 禮拜五除錯
	- STOP_? END 撌交郊嚗?蝔???"{""CH1_3"": -0.05}"  
	- 有時候會多一秒 ![[Pasted image 20260903111903.png|640]]
### 海辰容量計算方式修改
-  [x] 改成累計的形式進行紀錄+=log interval/3600*c_val
``` python
		def get_raw(ioa):
            return client.data_store.get(ioa, 0)

        v_val = get_raw(cfg.m.TOTAL_V)

        if v_val != 0:
            charge_cap = get_raw(cfg.m.SINGLE_CHARGE_POWER) * 1000 / v_val
            discharge_cap = get_raw(cfg.m.SINGLE_DISCHARGE_POWER) * 1000 / v_val

        else:
            charge_cap = 0.0
            discharge_cap = 0.0

        if client.connected:
            client.data_store[99991] = charge_cap
            client.data_store[99992] = discharge_cap

        st.metric("單次充電容量", f"{charge_cap:.1f} Ah")
        st.metric("單次放電容量", f"{discharge_cap:.1f} Ah") 
```
``` python
		def get_raw(ioa):
            return client.data_store.get(ioa, 0)

        c_val = get_raw(cfg.m.TOTAL_C)

        if c_val >= 0.:
            charge_cap = user_pref['log_interval']/3600 * c_val
        else if c_va; <= 0:
            discharge_cap = user_pref['log_interval']/3600 * c_val
        else:
	        charge_cap = charge_cap
            discharge_cap = discharge_cap
	        

        if client.connected:
            client.data_store[99991] += charge_cap
            client.data_store[99992] += discharge_cap

        st.metric("單次充電容量", f"{client.data_store[99991]:.1f} Ah")
        st.metric("單次放電容量", f"{client.data_store[99992]:.1f} Ah") 
```

---
# 明日預期
---
# 代辦事項
### 測Modbus Canbus通訊
-  [ ] 加入解析方式 與畫面呈現
### 伺服器讀取與呈現
-  [ ] 稍微再修正 讓伺服器讀取更直觀
-  [ ] 多加測試並跟彥瑋討論
### GusBLE
-  [ ] 待阿傑這邊實驗測試完 並討論有無問題
-  [ ] 後續討論新版Spec 並跟Kevin確認內容
### 電動巴士Canbus
-  [ ] 測試log收集結果及壓差、溫差、門檻閘值
### GusBuy
-  [ ] 商品類別對一個 Item 跟 Price 還有 Pic 跟 Spec
-  [ ] 通知客人到貨
-  [ ] 業務流程後續要加進來
### 永續發展課程
-  [ ] 注意上課時間
	-  [x] 客戶永續經營 (2026-08-21 13:00)
	-  [ ] ESG 報告與 GRI 準則 (2026-09-04 13:00)
	-  [ ] 永續分組 (2026-09-11 13:00)
	-  [ ] 永續提案說明(2026-09-18 13:00)
	-  [ ] 提案送審 (2026-09-18 10:00)

| 項次  | 課程名稱           |   上課日期    |    上課時間     | 主講人        | 備註  |
| :-: | :------------- | :-------: | :---------: | :--------- | :-- |
|  1  | 客戶永續經營         |   8/21    | 13:30~14:30 | 策略長許志帆特助   |     |
|  2  | ESG 報告與 GRI 準則 |    9/4    | 13:30~14:30 | 永續發展室 Gary |     |
|  3  | 永續分組           |   9/11    | 13:30~14:30 | 永續發展室 Gary |     |
|  4  | 永續提案說明         |   9/18    | 13:30~14:30 | 永續發展室 Gary |     |
|  5  | 提案送審           | 9/18~10/2 | 13:30~14:30 | 永續發展室 Gary |     |

---