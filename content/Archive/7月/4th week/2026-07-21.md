# 今日項目
### GusBLE
-  [x] 理解Vanco的意見並思考做法
-  [x] 先定義需要注意之項目
	-  [x] 非用戶之藍牙連線處理方式
	-  [x] 藍牙通訊例外之回報方式
	-  [x] 藍牙通訊基本限制
		- 最大值：125 暫存器 250 bytes
		- 正常命令回覆：一發一收
-  [x] 把項目寫進新版Spec
-  [x] 測試新的 密碼觸發怎麼顯示
-  [x] 持續測試密碼輸入
-  [x] 實做快取清除
-  [x] 格式更正
-  [x] 整理清楚兩種密碼使用方式的差異跟優缺點
	- 詳細察看[[藍芽密碼使用差異]]
	- 主要目的 別人不知道只有內部知道
	-  [ ] Polling Response Format要改
		-  Start code B0 func 01  CRC CRC
		-  Start code B0 func 01 Lenth XX payload 01 CRC CRC
-  [x] 廣播功能可用
-  [ ] 廣播搜尋自動輸入密碼
	- 後續行為如之前討論
	- B0 01 47 55 53 31 32 33 34 35 36 37
	- 3344
-  [ ] 研討新版Spec
### 碘鑽 UPS
-  [x] HMI 加入18 cells的判定方式
### GusBuy
-  [x] 理流程
	-  [x] 註冊 認證
	-  [x] 下單
	-  [x] 業務接洽
	-  [x] 上架
	-  [x] 出貨
		-  ( 疑問太多不提前規劃 ) 
	-  [x] 退貨
-  [x] 先測試Appwrite身分認證的方式
-  [x] 與市面商店對標比較
-  [x] 進一步規劃
-  [x] 畫心智圖
-  [x] 實際串接入認證API
-  [x] 梳理登入流程順暢
-  [x] 串接後台登入跟內部認證流程
-  [x] 讓前端內部網頁可以去修改商品資料
-  [x] 讓前端可以分配類別
-  [x] 製作分類專屬的頁面
-  [x] 規劃管理帳號的方法與實作
-  [x] 先給哈哥看商品詳細頁
-  [x] 只要商品資訊有更改 要記得在退出返回的時候提醒是否存取
-  [x] 線上發問 登入拿mail帳號 => 發問 => Webhook => Teams => 某個人 => 回復
	- https://energy.auo.com/zh-TW/Contact_Us
-  [ ] 商品類別對一個 Item 跟 Price 還有 Pic 跟 Spec
-  [ ] RWD 自適應畫面
-  [ ] 通知客人到貨
-  [ ] 業務流程後續要加進來
### 分選電芯 (根據Hioki的格式修改)
-  [x] 分選電芯順序要改 Barcode Voltage Resistance
-  [x] IR內阻倍率修改 x1000 不用了 歐姆改成m歐姆
-  [x] 改看看可不可以吃.csv輸出還是.xlsx
### Hioki
-  [x] 電壓內阻上下限
-  [x] 增加工站選擇 Core(Core) Pack(Pack) Cell(Cell)
-  [x] 改payload
DAQ
``` json
var payload = new Dictionary<string, object>
{
    ["stage"] = "daq970cellmeasure",
    ["tag"] = new Dictionary<string, object>
    {
        ["orderId"] = AppContext.WorkOrderId
    },
    ["tagfield"] = new Dictionary<string, object>
    {
        ["corepackBarcode"] = (zoneBarcodes.ContainsKey(z) ? zoneBarcodes[z] : $"Zone{z}_{DateTime.Now:HHmmss}")?.ToString(),
        ["Result"] = recentResultList[uploadIndex][z]
    },
    ["voltagemaxthread"] = double.Parse(txtVoltageMax.Text),
    ["voltageminthread"] = double.Parse(txtVoltageMin.Text),
    ["voltagediffthread"] = double.Parse(txtVoltageDiff.Text),
    ["tempmaxthread"] = double.Parse(txtTempMax.Text),
    ["tempminthread"] = double.Parse(txtTempMin.Text),
};
```
Hioki
``` json
var payloadObject = new
{
    stage = "Hioki_Scan",

    tagfield = new
    {
        device = fakeMode ? "BT3563A_FAKE" : "BT3563A",
        corepackBarcode = barcode
        Result =
    },

    voltage = volt,
    resistance = res,
    voltagemaxthread =
    voltageminthread =
    resistancemaxthread =
    resistanceminthread =

    TimeStamp = DateTime.Now.ToString("yyyy-MM-ddTHH:mm:ssK")
};
```
CellSort
``` json
var payloadObject = new
{
    stage = "CellSort",

    tagfield = new
    {
        corepackBarcode = corepackBarcode,
        cellBarcode = cellBarcode,
    },

    voltage = volt,
    resistance = res,
    date = date,
    deltav = deltav,
    deltaday = deltaday,
    voltagemaxthread = voltageLimit,
    resistancemaxthread = irlimit,
    daymaxthread = daylimit
};
```
LearningMachine
``` json
var payloadObject = new
{
    stage = "LearningMachine",

    tagfield = new
    {
        corepackBarcode = corepackBarcode,
        cellBarcode = cellBarcode,
        packBarcode = packBarcode,
    },
	fileName = fileName
	AH = AH,
	deltaV(mV)=deltaV,
	V1(mV)=V1,
	V2(mV)=V2,
	...
};
```

### Learning機上傳軟體
-  [ ] 可以設定容量上下限
-  [ ] 可以切換模式 變成Delta V
-  [ ] 可以設定電壓差上下限
-  [ ] 可以設定欄位 多列(壓差) or 單列(容量)
-  [ ] 讀 csv上傳 並讀取對應列來上傳
Index to 列
### 伺服器讀取與呈現
-  [ ] 讀取所有的Stage (measurement)
-  [ ] 伺服器加入開放式API 不鎖定項目
-  [ ] 數據項目 X軸時間 Y軸為開放式格式
-  [ ] 每個 Y軸都可以模糊搜尋 並要可以多項模糊搜尋
-  [ ] 可以匯出CSV
### 分選
-  [x] 00000 ~ 99999 ex: GU542612 00000 ~ GU542612 99999
-  [x] 可以設定前綴
-  [x] 可以設定流水起始數值
-  [x] 開始結束
-  [x] 上傳功能
-  [x] 日期不勾選連解析都不解析
	- 台塑新智能 不用解析日期 防報錯

---
# 明日預期
### GusBuy
-  [x] 線上發問 登入拿mail帳號 => 發問 => Webhook => Teams => 某個人 => 回復
	- https://energy.auo.com/zh-TW/Contact_Us
---
# 代辦事項
### 格斯總部261電池貨櫃後端Survey
-  [ ] 伺服器架構整理建構
### 格斯商城
-  [x] 伺服器架構建置
### RC 1樓 展示廳規劃
-  [ ] 等電視跟壁掛再叫蔡老闆幫忙安裝
---